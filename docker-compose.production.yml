
volumes:
  pg_data:
  static:
  media:

services:
  postgres:
    image: postgres:13
    container_name: postgres
    env_file:
      - kitty.env
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    image: maxagava/kittygram_backend:latest
    container_name: backend
    env_file:
      - kitty.env
    volumes:
      - static:/backend_static
      - media:/media
    depends_on:
      - postgres

  frontend:
    image: maxagava/kittygram_frontend:latest
    container_name: frontend
    env_file:
      - kitty.env
    volumes:
      - static:/app/build/static

  gateway:
    image: maxagava/kittygram_gateway:latest
    container_name: gateway
    ports:
      - "9000:80"
    volumes:
      - static:/backend_static
      - media:/media
    depends_on:
      - backend
      - frontend
